{
  "name": "image-grabber",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.config.sidepanel.ts && node --import tsx build-scripts.ts",
    "dev": "npm run build && echo 'Build complete. Load dist/ in chrome://extensions'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.4.0",
    "tsx": "^4.0.0",
    "vite": "^5.4.0",
    "vite-plugin-static-copy": "^1.0.0"
  }
}
